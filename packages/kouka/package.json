{
  "name": "kouka",
  "version": "0.0.0",
  "description": "A TypeScript standard library",
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": "./build/index.js"
  },
  "files": [
    "build",
    "src"
  ],
  "scripts": {
    "build": "tsgo -p tsconfig.build.json",
    "dev": "tsgo -p tsconfig.build.json --watch",
    "check:types": "tsgo --noEmit"
  },
  "dependencies": {
    "@kouka/core": "workspace:*"
  }
}
