/**
 * kouka - A TypeScript standard library.
 *
 * This is the main entry point that re-exports all modules from @kouka/core.
 *
 * @example
 * ```ts
 * import { Arr, Str, Obj, Fn } from 'kouka'
 *
 * const result = Arr.map([1, 2, 3], n => n * 2)
 * ```
 *
 * @packageDocumentation
 */

export * from '@kouka/core'
