{
  "name": "@kouka/semver",
  "version": "0.0.0",
  "type": "module",
  "sideEffects": false,
  "imports": {
    "#semver": "./build/_.js",
    "#semver/*": "./build/*.js"
  },
  "exports": {
    ".": "./build/_.js"
  },
  "dependencies": {
    "@vltpkg/semver": "^1.0.0-rc.12",
    "effect": "^3.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "check:types": "tsc --noEmit"
  }
}
